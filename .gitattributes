# Normalize line endings across platforms.
* text=auto eol=lf

# Windows batch + the Gradle wrapper script need specific endings.
*.bat text eol=crlf
gradlew text eol=lf

# Binary files.
*.jar binary
*.png binary
*.jpg binary
